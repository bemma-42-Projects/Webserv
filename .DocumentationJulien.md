// CGI : sous-traiter le travail d'exécution du code PHP ou Python
    // le serveur doit lancer le vrai programme PHP installé sur le PC
    // lui donner les données du client (le body du POST)
    // et récupérer sa réponse (le texte HTML)
    // pour la renvoyer au navigateur

	// ATTENTION : mettre aussi dans GET
	// mais aussi apres findIndex, car l'index peut aussi etre du php !

	// Dans POST, on veut envoyer des données a un script CGI
	// ou upload un fichier
	// ou un formulaire, etc
	// si le routeur identifie un POST vers un fichier .php
	// Webserv doit extraire le body de la requête HTTP
	// il utilise la variable d'environnement CONTENT_LENGTH
	// pour dire au script qu'il va envoyer x octets
	// il écrit ce body directement dans l'entrée standard stdin du script CGI via un pipe
	// CGI lit les variables d'environnement directement sur son stdin
	// le script CGI (fils) doit recevoir les données, les lire
	// puis envoyer la réponse sur la sortie standard (stdout)
	// avant le execve, le serveur doit prendre ce nombre et l'injecter dans un tableau envp
	// avec CONTENT_LENGTH=42
	// 

    // script PHP : séparé du serveur
    // il faut créer alors 2 tuyaux avec pipe()
    
    // on va créer un tuyan pipe_in pour que le serveur y écrive le body_ de la requête
    // et pour que le script PHP le lise

    // et un tuyau pipe_out pour que le script y écrive sa réponse HTML
    // et pour que le serveur le lise

    // Attention : le serveur ne doit pas exécuter le script lui-même
    // sinon, il s'arrêtera de fonctionner
    // il faut créer un fork pour cloner le programme

    // le processus père sera le serveur (WebServ)
    // le processus fils sera le CGI

    // dans le bloc if (pid == 0) (le fils, le CGI)
    // on utilise dup2 pour brancher l'entrée standard (STDIN_FILENO) sur pipe_in
    // et la sortie standard (STDOUT_FILENO) sur pipe_out
    // puis on appelle execve("/usr/bin/php-cgi", args, envp)
    // le clone meurt alors et est remplacé par le script PHP

    // dans le bloc else (le père, le serveur)
    // on ferme les "bouts de tuyau" dont on ne se sert pas
    // on écrit le body_ dans le pipe_in
    // on attend que le fils termine son calcul avec waitpid()
    // on fait un read() sur le pipe_out pour récupérer la réponse complète générée par PHP
    // on la stocke dans la variable answer_

    // Attention : il faut envoyer char **envp
    // c'est obligatoire pour le CGI
    // le script PHP ne sait rien du client
    // il faut traduuire la classe Request sous la forme de variables d'environnement

	// DONE
    // 1 : remplacer les extensions en dur (php, py, pl)
    // les extensions CGI doivent etre dynamiques
    // il faut demander a l'objet Location
    // si l'URL se termine par l'extension attendue pour cette route...
    // c'est un CGI

    // 2 : préparer ce que l'on va envoyer au script PHP
    // Créer une fonction qui génère le char **envp
	// a : créer un vecteur tampon
	// CLE=VALEUR
	// dans un vecteur de chaine
	// le C++ va gérer la mémoire de ces chaines pendant la construction
	// avec std::vector
	// puis ajouter les headers spécifiques
	// si la requête est un POST
	// le CGI doit savoir combien d'octets lire sur l'entrée standard
	// puis allouer un tableau de pointeurs de la taille du vecteur + 1 et copier chaque chaine en memoire
	
	// ATTENTION : protocole CGI : tous les autres headers HTTP envoyés par le client
	// comme User-Agent, Cookie, etc
	// doivent être transmis au script
	// en ajoutant le préfixe HTTP_
	// en mettant les headers en majuscule
	// et en remplacant les tirets par des underscores
	// exemple :
	// Accept-Language: fr
	// doit devenir
	// HTTP_ACCEPT_LANGUAGE=fr
	// dans le vecteur
	// cette fonction doit prendre l'objet Request en paramètre
	// et retourner un char ** prêt à être utilisé

	// attention : il faut aussi parser la réponse HTTP
	// le script CGI ne renvoie pas une réponse HTTP complète
	// mais quelques entêtes (le Content Type par exemple), une ligne vide
	// puis le code HTML
	// Webserv doit récupérer ces morceaux, calculer la taille
	// et l'emballer dans une vraie réponse HTTP complète
	// il faut trouver le double retour à la ligne
	// \r\n\r\n
	// dans la chaine brute lue depuis le pipe_out
	// puis couper la chaine en deux
	// tout ce qui est avant l'index devient cgi_headers
	// tout ce qui est après devient cgi_body
	// il faut ensuite calculer le content length
	// avec cgi body length
	// pour générer un entête content length valide
	// sinon, le navigateur du client tournera en boucle en attendant la suite de la page
	// forger la réponse finale avec la ligne de statut HTTP/1.1 200 OK
	// y coller les cgi_headers
	// ajouter le content length
	// et terminer par le cgi-body

	// enfin, sécuriser les performances avec epoll